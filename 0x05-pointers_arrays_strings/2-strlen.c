#include "holberton.h"

/**
  * _strlen - returns the lenght of a string
  *
  * @s: string to get the lenght of
  *
  * Return: lenght of s
  */
int _strlen(char *s)
{
	int count;

	for (count = 0; s[count] != '\0'; count++)
	{
	}
	return (count);
}
